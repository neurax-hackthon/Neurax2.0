@import "tailwindcss";

:root {
  /* â”€â”€â”€ Premium White Theme Palette â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F8FAFC;
  --bg-tertiary: #F1F5F9;

  --text-primary: #0F172A;
  /* Slate 900 */
  --text-secondary: #475569;
  /* Slate 600 */
  --text-tertiary: #94A3B8;
  /* Slate 400 */

  --accent-primary: #0891b2;
  /* Cyan 600 */
  --accent-secondary: #059669;
  /* Emerald 600 */
  --accent-gradient: linear-gradient(to right, #0891b2, #059669);

  --border-light: #E2E8F0;
  --border-strong: #CBD5E1;

  --shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(15, 23, 42, 0.1), 0 2px 4px -2px rgba(15, 23, 42, 0.05);
  --shadow-lg: 0 10px 15px -3px rgba(15, 23, 42, 0.1), 0 4px 6px -4px rgba(15, 23, 42, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(15, 23, 42, 0.1), 0 8px 10px -6px rgba(15, 23, 42, 0.05);

  --font-display: 'Orbitron', 'Space Grotesk', sans-serif;
  --font-body: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}

/* â”€â”€â”€ Base Reset â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  background-color: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-body);
  overflow-x: hidden;
  line-height: 1.5;
}

/* â”€â”€â”€ Custom Scrollbar â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--bg-secondary);
}

::-webkit-scrollbar-thumb {
  background: var(--border-strong);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--text-tertiary);
}

/* â”€â”€â”€ Typography â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
h1,
h2,
h3,
h4 {
  font-family: var(--font-display);
  color: var(--text-primary);
  font-weight: 700;
}

/* â”€â”€â”€ Global UI Components â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.section {
  padding: 48px 0;
  /* py-12 on mobile */
  position: relative;
}

@media (min-width: 768px) {
  .section {
    padding: 96px 0;
    /* py-24 on desktop */
  }
}

.section.alt {
  background-color: var(--bg-secondary);
}

.container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 16px;
  /* px-4 on mobile */
}

@media (min-width: 768px) {
  .container {
    padding: 0 32px;
    /* px-8 on desktop */
  }
}

.shadow-card {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 16px;
  box-shadow: var(--shadow-md);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.shadow-card:hover {
  transform: translateY(-5px);
  box-shadow: var(--shadow-lg);
}

.btn-primary {
  display: inline-block;
  padding: 14px 32px;
  background: var(--accent-gradient);
  color: #FFFFFF;
  border: none;
  border-radius: 100px;
  font-weight: 700;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 12px rgba(8, 145, 178, 0.2);
}

.btn-secondary {
  display: inline-block;
  padding: 14px 32px;
  background: #FFFFFF;
  color: var(--text-primary);
  border: 1px solid var(--border-strong);
  border-radius: 100px;
  font-weight: 700;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

.btn-secondary:hover {
  background: var(--bg-secondary);
  border-color: var(--accent-primary);
  transform: translateY(-2px);
}

/* â”€â”€â”€ Navigation â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 80px;
  display: flex;
  align-items: center;
  z-index: 1000;
  transition: all 0.3s ease;
  background: transparent;
}

.navbar.scrolled {
  height: 70px;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border-light);
  box-shadow: var(--shadow-sm);
}

.navbar-container {
  display: flex !important;
  align-items: center;
  justify-content: space-between;
  width: 100%;
}

.nav-logo {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 800;
  text-decoration: none;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: 12px;
}

.logo-img {
  height: 44px;
  width: auto;
  object-fit: contain;
  flex-shrink: 0;
  filter: drop-shadow(0 0 8px rgba(8, 145, 178, 0.3));
  transition: all 0.3s ease;
  display: block;
}

.nav-logo:hover .logo-img {
  transform: scale(1.1);
  filter: drop-shadow(0 0 12px rgba(8, 145, 178, 0.5));
}

@media (min-width: 768px) {
  .logo-img {
    height: 56px;
  }
}

.nav-content {
  display: flex;
  align-items: center;
  gap: 40px;
}

.nav-links {
  display: flex;
  gap: 32px;
  list-style: none;
}

.nav-links a {
  text-decoration: none;
  color: var(--text-secondary);
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.2s ease;
}

.nav-links a:hover {
  color: var(--accent-primary);
}

.nav-cta {
  padding: 10px 24px !important;
  font-size: 0.85rem !important;
}

@media (max-width: 1024px) {
  .nav-content {
    display: none;
  }
}

/* â”€â”€â”€ Global Hover Utilities â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.hover-lift {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.hover-lift:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}

.hover-glow:hover {
  box-shadow: 0 0 20px rgba(8, 145, 178, 0.2);
}

.live-dot {
  width: 10px;
  height: 10px;
  background: var(--accent-primary);
  border-radius: 50%;
  box-shadow: 0 0 10px rgba(8, 145, 178, 0.3);
}

#scroll-progress {
  position: fixed;
  top: 0;
  left: 0;
  height: 3px;
  background: var(--accent-gradient);
  z-index: 9999;
}

/* â”€â”€â”€ Section Header â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.section-header {
  text-align: center;
  margin-bottom: 64px;
}

.section-tag {
  display: inline-block;
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--accent-primary);
  margin-bottom: 12px;
}

.section-title {
  font-family: var(--font-display);
  font-size: clamp(2.5rem, 5vw, 3.5rem);
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.1;
}

.section-title span {
  background: var(--accent-gradient);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.section-line {
  width: 80px;
  height: 4px;
  background: var(--accent-gradient);
  margin: 20px auto 0;
  border-radius: 2px;
}

.section-desc {
  color: var(--text-secondary);
  font-size: 1.1rem;
  max-width: 650px;
  margin: 20px auto 0;
  line-height: 1.7;
}

/* â”€â”€â”€ Hero Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.hero-section {
  min-height: 85vh;
  /* Reduced for mobile */
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  background: #FFFFFF;
}

@media (min-width: 768px) {
  .hero-section {
    min-height: 100vh;
  }
}

#vanta-bg {
  position: absolute;
  inset: 0;
  z-index: 0;
}

.hero-content {
  position: relative;
  z-index: 2;
  text-align: center;
  max-width: 900px;
  width: 100%;
  padding: clamp(80px, 12vw, 100px) clamp(16px, 4vw, 24px) 60px;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(8, 145, 178, 0.08);
  border: 1px solid rgba(8, 145, 178, 0.2);
  color: var(--accent-primary);
  padding: 8px 20px;
  border-radius: 100px;
  font-size: 0.85rem;
  font-weight: 700;
  margin-bottom: 32px;
  box-shadow: 0 4px 12px rgba(8, 145, 178, 0.1);
}

.hero-meta {
  display: flex;
  gap: clamp(16px, 4vw, 32px);
  justify-content: center;
  align-items: center;
  margin-bottom: 24px;
  /* Reduced for mobile */
  flex-wrap: wrap;
}

@media (min-width: 768px) {
  .hero-meta {
    margin-bottom: 40px;
  }
}

.hero-meta-item {
  color: var(--text-secondary);
  font-weight: 600;
  font-size: clamp(0.9rem, 1.5vw, 1rem);
  display: flex;
  align-items: center;
  gap: 8px;
}

.hero-title {
  font-size: clamp(3.5rem, 8vw, 6.5rem);
  font-weight: 900;
  line-height: 0.9;
  letter-spacing: -2px;
  margin-bottom: 24px;
}

.hero-title span {
  background: var(--accent-gradient);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero-subtitle-wrapper {
  margin-bottom: 24px;
  /* Reduced for mobile */
  font-size: 1.1rem;
}

@media (min-width: 768px) {
  .hero-subtitle-wrapper {
    margin-bottom: 40px;
    font-size: 1.25rem;
  }
}

.hero-cta {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 8px;
}

/* â”€â”€â”€ About Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.about-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 24px;
  align-items: center;
}

.about-stats-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-top: 16px;
}

.about-intro {
  color: var(--text-secondary);
  line-height: 1.8;
  margin-bottom: 20px;
  font-size: 1rem;
}

.about-cards-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}

@media (min-width: 768px) {
  .about-grid {
    gap: 48px;
  }

  .about-stats-grid {
    gap: 20px;
  }

  .about-cards-grid {
    gap: 20px;
  }
}

/* â”€â”€â”€ Statistics Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 20px;
}

.stat-card {
  padding: 24px 16px;
  text-align: center;
}

@media (min-width: 768px) {
  .stats-grid {
    gap: 32px;
  }

  .stat-card {
    padding: 40px 24px;
  }
}

/* â”€â”€â”€ Themes Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.themes-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 20px;
}

.theme-card {
  background: #FFFFFF;
  border: 1px solid var(--border-light);
  border-radius: 20px;
  padding: 24px;
  cursor: pointer;
}


/* â”€â”€â”€ Why Join Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.why-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
  /* Reduced for mobile */
}

@media (min-width: 768px) {
  .why-grid {
    gap: 24px;
  }
}

/* â”€â”€â”€ Schedule Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.schedule-days-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 24px;
  /* Reduced for mobile */
  align-items: start;
}

.schedule-day-column {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 20px;
  /* Snugger for mobile */
  padding: 24px 20px;
  /* Reduced for mobile */
  box-shadow: var(--shadow-md);
}

@media (min-width: 768px) {
  .schedule-days-grid {
    gap: 40px;
  }

  .schedule-day-column {
    border-radius: 24px;
    padding: 40px;
  }
}

/* â”€â”€â”€ Prizes Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.podium-wrapper {
  display: flex;
  align-items: flex-end;
  justify-content: center;
  gap: 16px;
  /* Reduced for mobile */
  margin-top: 32px;
  /* Reduced for mobile */
  flex-wrap: wrap;
}

.podium-card {
  padding: 24px 20px;
  /* Reduced for mobile */
  text-align: center;
  flex: 1;
  min-width: min(280px, 100%);
  max-width: 320px;
}

.podium-card.first {
  max-width: 360px;
  background: #FFFFFF;
  border: 2px solid #FFD700;
  border-radius: 24px;
  box-shadow: 0 20px 40px rgba(255, 215, 0, 0.25);
  position: relative;
  order: -1;
  /* Show first on mobile */
}

@media (min-width: 768px) {
  .podium-wrapper {
    gap: 24px;
    margin-top: 60px;
  }

  .podium-card {
    padding: 40px 32px;
  }

  .podium-card.first {
    order: 0;
    /* Natural order for podium effect on desktop */
  }
}

.scroll-hint {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding-bottom: 40px;
}

.scroll-hint-text {
  font-size: 0.8rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 2px;
  color: var(--text-tertiary);
}

.scroll-hint-icon {
  font-size: 1.25rem;
  color: var(--accent-primary);
}

.gallery-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 12px;
  /* Reduced for mobile */
  align-items: stretch;
}

.gallery-right {
  display: flex;
  flex-direction: column;
  gap: 12px;
  /* Reduced for mobile */
}

@media (min-width: 768px) {
  .gallery-grid {
    gap: 16px;
  }

  .gallery-right {
    gap: 16px;
  }
}

/* â”€â”€â”€ Sponsors Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.sponsors-ticker {
  margin-bottom: 16px;
  overflow: hidden;
  padding: 8px 0;
}

.sponsor-logo {
  padding: 16px 32px;
  /* Reduced for mobile */
  font-size: 1.1rem;
  font-weight: 800;
  color: var(--text-tertiary);
  background: #FFFFFF;
  border-radius: 12px;
  margin-right: 16px;
  border: 1px solid var(--border-light);
  transition: all 0.3s ease;
  cursor: pointer;
}

.sponsors-group {
  margin-bottom: 24px;
}

.sponsors-group.second {
  margin-bottom: 32px;
}

@media (min-width: 768px) {
  .sponsors-ticker {
    margin-bottom: 24px;
    padding: 12px 0;
  }

  .sponsor-logo {
    padding: 20px 48px;
    font-size: 1.25rem;
    border-radius: 16px;
    margin-right: 24px;
  }

  .sponsors-group {
    margin-bottom: 32px;
  }

  .sponsors-group.second {
    margin-bottom: 48px;
  }
}

.revealing-soon-container {
  max-width: 800px;
  margin: 40px auto 0;
  padding: 60px 20px;
  background: rgba(255, 255, 255, 0.4);
  backdrop-filter: blur(10px);
  border: 1px dashed var(--border-strong);
  border-radius: 32px;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.revealing-soon-container::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(45deg,
      transparent 25%,
      rgba(8, 145, 178, 0.03) 50%,
      transparent 75%);
  background-size: 200% 200%;
  animation: shimmer 8s linear infinite;
}

.revealing-soon-title {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 900;
  background: linear-gradient(to right, #0891b2, #059669, #0891b2);
  background-size: 200% auto;
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: shine 4s linear infinite;
  margin-bottom: 8px;
}

.revealing-soon-subtitle {
  color: var(--text-secondary);
  font-size: 1.1rem;
  font-weight: 500;
  letter-spacing: 4px;
  text-transform: uppercase;
  margin-bottom: 24px;
}

.pulse-dot {
  width: 12px;
  height: 12px;
  background: var(--accent-primary);
  border-radius: 50%;
  margin: 0 auto;
  position: relative;
}

.pulse-dot::after {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  border: 2px solid var(--accent-primary);
  animation: pulse-ring 2s cubic-bezier(0.25, 0.46, 0.45, 0.94) infinite;
}

@keyframes shine {
  to {
    background-position: 200% center;
  }
}

@keyframes shimmer {
  0% {
    background-position: -200% -200%;
  }

  100% {
    background-position: 200% 200%;
  }
}

@keyframes pulse-ring {
  0% {
    transform: scale(0.5);
    opacity: 0.8;
  }

  80%,
  100% {
    transform: scale(2.5);
    opacity: 0;
  }
}

/* â”€â”€â”€ FAQ Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.faq-search {
  max-width: 600px;
  margin: 0 auto 24px;
  position: relative;
}

.faq-tabs {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin-bottom: 24px;
  flex-wrap: wrap;
}

.faq-item-header {
  padding: 20px 24px;
  background: #FFFFFF;
  border: 1px solid var(--border-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  transition: all 0.3s ease;
}

.faq-list {
  max-width: 800px;
  margin: 0 auto;
}

.faq-bottom-cta {
  text-align: center;
  margin-top: 32px;
  /* Reduced for mobile */
}

@media (min-width: 768px) {
  .faq-search {
    margin-bottom: 40px;
  }

  .faq-tabs {
    margin-bottom: 48px;
  }

  .faq-item-header {
    padding: 24px 32px;
  }

  .faq-bottom-cta {
    margin-top: 64px;
  }
}

/* â”€â”€â”€ Contact Section â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.contact-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 20px;
}

.contact-card {
  background: #FFFFFF;
  border: 1px solid var(--border-light);
  border-radius: 24px;
  padding: 24px;
  /* Reduced for mobile */
  text-align: center;
}

.register-cta-wrapper {
  text-align: center;
  margin-top: 40px;
  /* Reduced for mobile */
  margin-bottom: 24px;
}

@media (min-width: 768px) {
  .contact-grid {
    gap: 32px;
  }

  .contact-card {
    padding: 40px;
  }

  .register-cta-wrapper {
    margin-top: 100px;
    margin-bottom: 40px;
  }
}

/* â”€â”€â”€ Countdown â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.countdown-grid {
  display: flex;
  gap: 20px;
  justify-content: center;
  margin: 40px 0;
  flex-wrap: wrap;
}

.countdown-card {
  background: var(--bg-secondary);
  border: 1px solid var(--border-light);
  border-radius: 16px;
  padding: 24px;
  text-align: center;
  min-width: 100px;
  box-shadow: var(--shadow-sm);
}

.countdown-number {
  font-family: var(--font-display);
  font-size: 3rem;
  font-weight: 900;
  color: var(--text-primary);
  line-height: 1;
}

.countdown-label {
  font-size: 0.75rem;
  color: var(--text-tertiary);
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-top: 8px;
  font-weight: 700;
}



/* â”€â”€â”€ Animations â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@keyframes fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes marquee {
  0% {
    transform: translateX(0);
  }

  100% {
    transform: translateX(-50%);
  }
}

.animate-fadeIn {
  animation: fadeIn 0.8s ease-out forwards;
}

.animate-slideUp {
  animation: slideUp 0.8s ease-out forwards;
}

/* â”€â”€â”€ Utility â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.text-accent {
  color: var(--accent-primary);
}

.bg-alt {
  background-color: var(--bg-secondary);
}

/* Legacy glass-card mapping */
.glass-card {
  background: var(--bg-primary);
  border: 1px solid var(--border-light);
  border-radius: 16px;
  box-shadow: var(--shadow-md);
}

/* â”€â”€â”€ Terminal Cursor (TerminalTyper component) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@keyframes blink {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0;
  }
}

.terminal-cursor {
  display: inline-block;
  width: 2px;
  height: 1em;
  background: currentColor;
  margin-left: 2px;
  vertical-align: text-bottom;
  animation: blink 1s step-end infinite;
}

/* â”€â”€â”€ Modal Overlay â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.modal-overlay {
  position: fixed;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  padding: 16px;
}

/* â”€â”€â”€ Hamburger Button â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.hamburger {
  display: none;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 5px;
  width: 44px;
  height: 44px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  z-index: 1100;
}

.hamburger span {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--text-primary);
  border-radius: 2px;
  transition: transform 0.3s ease, opacity 0.3s ease;
  transform-origin: center;
}

.hamburger.open span:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}

.hamburger.open span:nth-child(2) {
  opacity: 0;
}

.hamburger.open span:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

/* â”€â”€â”€ Mobile Nav Overlay â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.mobile-nav {
  position: fixed;
  inset: 0;
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(12px);
  z-index: 1050;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 24px;
  padding: 80px 24px 40px;
}

.mobile-nav a {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--text-primary);
  text-decoration: none;
  transition: color 0.2s ease;
}

.mobile-nav a:hover {
  color: var(--accent-primary);
}

.mobile-nav .btn-primary {
  width: 100%;
  max-width: 280px;
  text-align: center;
  margin-top: 16px;
}

/* â”€â”€â”€ Mobile Responsive â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@media (max-width: 1023px) {
  .hamburger {
    display: flex;
  }
}

@media (max-width: 767px) {

  /* Section Header */
  .section-title {
    font-size: clamp(1.75rem, 7vw, 2.5rem);
  }

  .section-desc {
    font-size: 1rem;
  }

  /* Hero */
  .hero-section {
    min-height: 100svh;
    min-height: 100dvh;
    /* dynamic viewport height for mobile browsers */
    max-height: none;
  }

  .hero-content {
    padding: 90px 16px 48px;
  }

  .hero-title {
    font-size: clamp(2.5rem, 14vw, 4rem);
    letter-spacing: -1px;
    margin-bottom: 16px;
  }

  .hero-subtitle-wrapper {
    font-size: 1rem;
    margin-bottom: 24px;
  }

  .hero-meta {
    flex-wrap: wrap !important;
    gap: 12px 20px !important;
    justify-content: center !important;
    font-size: 0.9rem;
  }

  .hero-cta {
    flex-direction: column;
    align-items: stretch;
    gap: 12px;
  }

  .hero-cta a,
  .hero-cta button {
    width: 100%;
    text-align: center;
    justify-content: center;
  }

  /* Countdown */
  .countdown-grid {
    gap: 12px;
    margin: 24px 0;
  }

  .countdown-card {
    padding: 16px 12px;
    min-width: 72px;
  }

  .countdown-number {
    font-size: 2rem;
  }

  /* Timeline â€” vertical stack on mobile */
  .timeline-wrapper {
    padding: 20px 0 !important;
  }

  .timeline-line-container {
    display: none !important;
  }

  .timeline-items {
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 0 !important;
    border-left: 2px solid var(--bg-tertiary);
    padding-left: 24px;
    margin-left: 24px;
  }

  .timeline-item {
    text-align: left !important;
    flex: none !important;
    width: 100%;
    padding-bottom: 32px;
    position: relative !important;
  }

  .timeline-node {
    position: absolute !important;
    left: -44px !important;
    top: 0 !important;
    margin: 0 !important;
    width: 40px !important;
    height: 40px !important;
    font-size: 1.1rem !important;
  }

  /* Prizes â€” stack cleanly, remove vertical offset */
  .podium-card.first {
    transform: none !important;
  }

  /* Schedule card padding */
  .schedule-day-column {
    padding: 24px !important;
  }

  /* Navbar */
  .nav-logo {
    font-size: 1.1rem;
  }

  /* Footer bottom bar */
  .footer>.container>div:last-child {
    flex-direction: column;
    text-align: center;
  }

  /* Register CTA box in Contact */
  .register-cta-box {
    padding: 36px 20px !important;
  }

  /* Gallery â€” collapse to single column on mobile */
  .gallery-grid {
    grid-template-columns: 1fr !important;
  }

  .gallery-right {
    flex-direction: column !important;
  }

  .gallery-right>div {
    aspect-ratio: 16/9;
    min-height: unset !important;
  }

  /* About â€” consolidated into main about-grid class */

  /* Prizes â€” prevent card overflow on narrow phones */
  .podium-card {
    min-width: min(260px, 90vw) !important;
    padding: 28px 20px !important;
  }

  /* Schedule â€” reduce card padding on mobile */
  .schedule-day-column {
    padding: 24px 20px !important;
    border-radius: 16px !important;
  }

  /* Contact â€” reduce register CTA spacing */
  .register-cta-box {
    padding: 36px 20px !important;
    border-radius: 20px !important;
  }

}

@media (max-width: 479px) {
  .hero-title {
    font-size: clamp(2.2rem, 13vw, 3rem);
  }

  .countdown-card {
    min-width: 64px;
    padding: 14px 10px;
  }

  .countdown-number {
    font-size: 1.75rem;
  }

  /* About â€” sub grids too tight at 320px */
  .about-grid>div>div[style*="grid-template-columns: 1fr 1fr"],
  .about-grid div[style*="1fr 1fr"] {
    grid-template-columns: 1fr !important;
  }

  /* Themes modal â€” prevent padding overflow on 320px */
  .modal-box {
    padding: 28px 20px !important;
    border-radius: 16px !important;
  }

  /* Contact register CTA headline */
  .register-cta-box h2 {
    font-size: clamp(1.5rem, 7vw, 2.5rem) !important;
  }

  /* Prizes special awards â€” allow full width */
  .podium-card {
    max-width: 100% !important;
  }
}

/* â”€â”€â”€ Tablet range (768px â€“ 1023px) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@media (min-width: 768px) and (max-width: 1023px) {
  .section {
    padding: clamp(60px, 6vw, 80px) 0;
  }

  .container {
    padding: 0 32px;
  }
}

/* --- Gallery Shimmer ----------------------------------------------------- */
.animate-shimmer-sweep {
  animation: shimmer-sweep 2s infinite;
}

@keyframes shimmer-sweep {
  0% {
    transform: translateX(-100%);
  }

  100% {
    transform: translateX(100%);
  }
}

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   HACKATHON LAUNCH SYSTEM â€” CSS
   All styles below power the admin panel, neon timer, intro overlay,
   live banner, and launch effects. No existing styles are modified.
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */

/* â”€â”€â”€ Neon Hackathon 24h Timer â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.countdown-neon-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  margin: 40px 0;
}

.countdown-neon-label {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--accent-primary);
  opacity: 0.85;
}

.countdown-neon-grid {
  display: flex;
  align-items: center;
  gap: 0;
}

.countdown-neon-group {
  display: flex;
  align-items: center;
  position: relative;
}

.countdown-neon-card {
  background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  border: 1px solid rgba(8, 145, 178, 0.4);
  border-radius: 12px;
  padding: 20px 24px;
  text-align: center;
  box-shadow:
    0 0 20px rgba(8, 145, 178, 0.15),
    0 0 40px rgba(8, 145, 178, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
  position: relative;
  overflow: hidden;
}

.countdown-neon-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(to right, transparent, rgba(8, 145, 178, 0.6), transparent);
}

.countdown-neon-number {
  font-family: var(--font-display);
  font-size: clamp(2.5rem, 7vw, 4.5rem);
  font-weight: 900;
  line-height: 1;
  background: linear-gradient(to bottom, #7dd3fc, #0891b2);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  display: block;
  letter-spacing: -2px;
}

.countdown-neon-unit {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: rgba(8, 145, 178, 0.7);
  text-align: center;
  margin-top: 6px;
  position: absolute;
  bottom: -20px;
  left: 50%;
  transform: translateX(-50%);
  white-space: nowrap;
}

.countdown-neon-sep {
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 900;
  color: rgba(8, 145, 178, 0.5);
  padding: 0 8px;
  align-self: center;
  margin-bottom: 4px;
  animation: sep-blink 1s ease-in-out infinite;
}

@keyframes sep-blink {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0.3;
  }
}

.neon-pulse {
  animation: neon-pulse 2s ease-in-out infinite;
}

@keyframes neon-pulse {

  0%,
  100% {
    text-shadow:
      0 0 10px rgba(8, 145, 178, 0.8),
      0 0 20px rgba(8, 145, 178, 0.4);
  }

  50% {
    text-shadow:
      0 0 20px rgba(8, 145, 178, 1),
      0 0 40px rgba(8, 145, 178, 0.6),
      0 0 60px rgba(8, 145, 178, 0.2);
  }
}

.countdown-neon-ended {
  font-family: var(--font-display);
  font-size: 2rem;
  font-weight: 900;
  color: var(--accent-primary);
  letter-spacing: 2px;
  text-align: center;
}

/* â”€â”€â”€ Glitch Text Animation â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.glitch-text {
  position: relative;
}

.glitch-text::before,
.glitch-text::after {
  content: attr(data-text);
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: transparent;
}

.glitch-text::before {
  color: #0891b2;
  animation: glitch-1 3.5s infinite linear;
  clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
}

.glitch-text::after {
  color: #059669;
  animation: glitch-2 3.5s infinite linear;
  clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
}

@keyframes glitch-1 {

  0%,
  92%,
  100% {
    transform: none;
    opacity: 1;
  }

  93% {
    transform: translate(-3px, 1px);
    opacity: 0.8;
  }

  95% {
    transform: translate(3px, -1px);
    opacity: 0.8;
  }

  97% {
    transform: translate(-2px, 2px);
    opacity: 0.9;
  }
}

@keyframes glitch-2 {

  0%,
  90%,
  100% {
    transform: none;
    opacity: 1;
  }

  91% {
    transform: translate(3px, -2px);
    opacity: 0.8;
  }

  93% {
    transform: translate(-3px, 1px);
    opacity: 0.8;
  }

  96% {
    transform: translate(2px, -1px);
    opacity: 0.9;
  }
}

/* â”€â”€â”€ Launch Intro Overlay â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.launch-intro-overlay {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #000913;
  cursor: pointer;
  overflow: hidden;
}

.intro-grid-bg {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(8, 145, 178, 0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(8, 145, 178, 0.06) 1px, transparent 1px);
  background-size: 60px 60px;
  animation: grid-scroll 12s linear infinite;
}

@keyframes grid-scroll {
  0% {
    transform: translateY(0);
  }

  100% {
    transform: translateY(60px);
  }
}

.intro-radial-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 600px;
  height: 600px;
  background: radial-gradient(circle, rgba(8, 145, 178, 0.18) 0%, transparent 70%);
  pointer-events: none;
}

.intro-content {
  position: relative;
  z-index: 2;
  text-align: center;
  padding: 40px 24px;
  max-width: 800px;
  width: 100%;
}

.intro-started-label {
  font-family: var(--font-mono);
  font-size: clamp(1rem, 2.5vw, 1.4rem);
  font-weight: 700;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: #0891b2;
  margin-bottom: 24px;
  text-shadow: 0 0 20px rgba(8, 145, 178, 0.8);
}

.intro-title {
  font-family: var(--font-display);
  font-size: clamp(3.5rem, 12vw, 9rem);
  font-weight: 900;
  line-height: 1;
  background: linear-gradient(135deg, #7dd3fc 0%, #0891b2 40%, #059669 70%, #34d399 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: -3px;
  margin-bottom: 24px;
}

.intro-tagline {
  font-family: var(--font-mono);
  font-size: clamp(0.75rem, 1.5vw, 1rem);
  letter-spacing: 3px;
  text-transform: uppercase;
  color: rgba(125, 211, 252, 0.6);
  margin-bottom: 32px;
}

.intro-divider {
  width: 200px;
  height: 2px;
  background: linear-gradient(to right, transparent, #0891b2, transparent);
  margin: 0 auto 24px;
  transform-origin: center;
}

.intro-dismiss-hint {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 2px;
  color: rgba(125, 211, 252, 0.4);
  text-transform: uppercase;
}

.intro-scanlines {
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(to bottom,
      transparent 0px,
      transparent 3px,
      rgba(0, 0, 0, 0.06) 3px,
      rgba(0, 0, 0, 0.06) 4px);
  pointer-events: none;
}

/* â”€â”€â”€ Live Banner â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.live-launch-banner {
  position: fixed;
  top: 80px;
  left: 0;
  right: 0;
  z-index: 990;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 10px 16px;
  background: linear-gradient(to right, #0c1a26, #0f2a1e, #0c1a26);
  border-bottom: 1px solid rgba(8, 145, 178, 0.3);
  box-shadow: 0 4px 20px rgba(8, 145, 178, 0.2);
  overflow: hidden;
}

.live-launch-banner::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, transparent 0%, rgba(8, 145, 178, 0.06) 50%, transparent 100%);
  background-size: 200% 100%;
  animation: banner-shimmer 3s linear infinite;
}

@keyframes banner-shimmer {
  0% {
    background-position: -200% 0;
  }

  100% {
    background-position: 200% 0;
  }
}

.live-banner-text {
  font-family: var(--font-mono);
  font-size: clamp(0.7rem, 1.5vw, 0.85rem);
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: #7dd3fc;
  text-shadow: 0 0 12px rgba(8, 145, 178, 0.6);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.live-banner-text strong {
  color: #0891b2;
  text-shadow:
    0 0 10px rgba(8, 145, 178, 1),
    0 0 20px rgba(8, 145, 178, 0.5);
}

.live-banner-pulse {
  display: inline-block;
  width: 8px;
  height: 8px;
  background: #0891b2;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 0 8px rgba(8, 145, 178, 0.8);
  animation: live-dot-pulse 1.5s ease-in-out infinite;
}

@keyframes live-dot-pulse {

  0%,
  100% {
    box-shadow: 0 0 8px rgba(8, 145, 178, 0.8);
    transform: scale(1);
  }

  50% {
    box-shadow: 0 0 16px rgba(8, 145, 178, 1);
    transform: scale(1.3);
  }
}

/* â”€â”€â”€ Floating Admin FAB â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.admin-fab {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 9000;
  width: 52px;
  height: 52px;
  border-radius: 50%;
  border: none;
  background: rgba(15, 23, 42, 0.85);
  backdrop-filter: blur(12px);
  color: white;
  font-size: 1.3rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow:
    0 4px 16px rgba(0, 0, 0, 0.3),
    0 0 0 1px rgba(8, 145, 178, 0.2);
  transition: box-shadow 0.3s ease;
}

.admin-fab:hover {
  box-shadow:
    0 8px 24px rgba(0, 0, 0, 0.4),
    0 0 0 2px rgba(8, 145, 178, 0.5);
}

/* â”€â”€â”€ Admin Panel â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.admin-panel {
  position: fixed;
  bottom: 88px;
  right: 24px;
  z-index: 9000;
  width: 300px;
  background: rgba(10, 18, 32, 0.96);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(8, 145, 178, 0.25);
  border-radius: 20px;
  box-shadow:
    0 20px 60px rgba(0, 0, 0, 0.5),
    0 0 0 1px rgba(8, 145, 178, 0.1);
  overflow: hidden;
}

.admin-panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  border-bottom: 1px solid rgba(8, 145, 178, 0.15);
}

.admin-panel-title {
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 700;
  color: rgba(125, 211, 252, 0.9);
  letter-spacing: 1px;
}

.admin-close-btn {
  background: none;
  border: none;
  color: rgba(148, 163, 184, 0.6);
  cursor: pointer;
  font-size: 0.9rem;
  padding: 4px 8px;
  border-radius: 6px;
  transition: color 0.2s;
  line-height: 1;
}

.admin-close-btn:hover {
  color: rgba(148, 163, 184, 1);
  background: rgba(255, 255, 255, 0.05);
}

/* â”€â”€â”€ Admin Login Form â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.admin-login-form {
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.admin-login-label {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  font-weight: 600;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: rgba(148, 163, 184, 0.6);
  margin-bottom: 4px;
}

.admin-input {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(8, 145, 178, 0.2);
  border-radius: 10px;
  padding: 10px 14px;
  color: #e2e8f0;
  font-size: 0.9rem;
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
  width: 100%;
}

.admin-input::placeholder {
  color: rgba(148, 163, 184, 0.35);
}

.admin-input:focus {
  border-color: rgba(8, 145, 178, 0.6);
  box-shadow: 0 0 0 3px rgba(8, 145, 178, 0.1);
}

.admin-error {
  font-size: 0.78rem;
  color: #f87171;
  text-align: center;
  padding: 6px 8px;
  background: rgba(239, 68, 68, 0.1);
  border-radius: 6px;
  border: 1px solid rgba(239, 68, 68, 0.2);
}

.admin-login-btn {
  background: linear-gradient(to right, #0891b2, #059669);
  color: white;
  border: none;
  border-radius: 10px;
  padding: 11px;
  font-weight: 700;
  font-size: 0.9rem;
  cursor: pointer;
  transition: opacity 0.2s, transform 0.2s;
  margin-top: 4px;
}

.admin-login-btn:hover:not(:disabled) {
  opacity: 0.9;
  transform: translateY(-1px);
}

.admin-login-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* â”€â”€â”€ Admin Dashboard â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.admin-dashboard,
.admin-noauth {
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 14px;
  align-items: center;
}

.admin-welcome {
  font-size: 0.82rem;
  color: rgba(148, 163, 184, 0.8);
  text-align: center;
  line-height: 1.5;
}

.admin-email-display {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: rgba(125, 211, 252, 0.8);
  word-break: break-all;
}

.admin-launched-badge {
  background: linear-gradient(to right, rgba(8, 145, 178, 0.15), rgba(5, 150, 105, 0.15));
  border: 1px solid rgba(8, 145, 178, 0.3);
  border-radius: 10px;
  padding: 12px 20px;
  color: #7dd3fc;
  font-weight: 700;
  font-size: 0.9rem;
  text-align: center;
  width: 100%;
  animation: neon-pulse 2s ease-in-out infinite;
}

.admin-logout-btn {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(148, 163, 184, 0.15);
  border-radius: 8px;
  color: rgba(148, 163, 184, 0.6);
  padding: 8px 16px;
  font-size: 0.8rem;
  cursor: pointer;
  transition: all 0.2s ease;
}

.admin-logout-btn:hover {
  background: rgba(255, 255, 255, 0.1);
  color: rgba(148, 163, 184, 0.9);
}

.admin-noauth p {
  font-size: 0.85rem;
  color: #f87171;
  text-align: center;
}

/* â”€â”€â”€ Launch Button â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.launch-btn {
  width: 100%;
  padding: 14px 20px;
  background: linear-gradient(135deg, #0891b2 0%, #059669 100%);
  border: none;
  border-radius: 12px;
  color: white;
  font-weight: 800;
  font-size: 1rem;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow:
    0 4px 20px rgba(8, 145, 178, 0.4),
    0 0 0 1px rgba(8, 145, 178, 0.3);
  transition: box-shadow 0.3s ease;
}

.launch-btn::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, transparent 50%);
  pointer-events: none;
}

.launch-btn:hover:not(:disabled) {
  box-shadow:
    0 8px 30px rgba(8, 145, 178, 0.6),
    0 0 0 2px rgba(8, 145, 178, 0.5),
    0 0 40px rgba(8, 145, 178, 0.2);
}

.launch-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.launch-btn-inner {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.launch-spinner {
  width: 16px;
  height: 16px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-top-color: white;
  border-radius: 50%;
  display: inline-block;
  animation: spin 0.7s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

/* â”€â”€â”€ Responsive overrides â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@media (max-width: 480px) {
  .admin-panel {
    width: calc(100vw - 32px);
    right: 16px;
    bottom: 80px;
  }

  .admin-fab {
    bottom: 16px;
    right: 16px;
    width: 46px;
    height: 46px;
    font-size: 1.1rem;
  }

  .countdown-neon-card {
    padding: 14px 16px;
  }

  .live-banner-text {
    font-size: 0.6rem;
    letter-spacing: 1px;
  }

  .intro-title {
    letter-spacing: -1px;
  }
}
/* --- Admin Status/Reset Styles --- */
.admin-status-badge{width:100%;padding:10px 16px;border-radius:10px;font-weight:700;font-size:.85rem;text-align:center}
.admin-status-badge.live{background:linear-gradient(to right,rgba(8,145,178,.15),rgba(5,150,105,.15));border:1px solid rgba(8,145,178,.35);color:#7dd3fc;animation:neon-pulse 2s ease-in-out infinite}
.admin-status-badge.idle{background:rgba(255,255,255,.04);border:1px solid rgba(148,163,184,.12);color:rgba(148,163,184,.6)}
.admin-divider{width:100%;height:1px;background:rgba(8,145,178,.12);margin:2px 0}
.admin-reset-btn{width:100%;padding:10px;background:rgba(239,68,68,.06);border:1px solid rgba(239,68,68,.2);border-radius:10px;color:rgba(252,165,165,.85);font-size:.82rem;font-weight:600;cursor:pointer;transition:all .2s}
.admin-reset-btn:hover:not(:disabled){background:rgba(239,68,68,.12);border-color:rgba(239,68,68,.4);color:#fca5a5}
.admin-reset-btn:disabled{opacity:.4;cursor:not-allowed}
.admin-confirm-reset{width:100%;padding:12px;background:rgba(239,68,68,.08);border:1px solid rgba(239,68,68,.2);border-radius:10px;display:flex;flex-direction:column;gap:10px}
.admin-confirm-text{font-size:.78rem;color:rgba(252,165,165,.85);text-align:center;line-height:1.4}
.admin-confirm-row{display:flex;gap:8px}
.admin-confirm-yes{flex:1;padding:8px;background:rgba(239,68,68,.2);border:1px solid rgba(239,68,68,.4);border-radius:8px;color:#fca5a5;font-weight:700;font-size:.78rem;cursor:pointer;transition:all .2s}
.admin-confirm-yes:hover:not(:disabled){background:rgba(239,68,68,.35)}
.admin-confirm-yes:disabled{opacity:.5;cursor:not-allowed}
.admin-confirm-no{flex:1;padding:8px;background:rgba(255,255,255,.04);border:1px solid rgba(148,163,184,.15);border-radius:8px;color:rgba(148,163,184,.6);font-weight:600;font-size:.78rem;cursor:pointer;transition:all .2s}
.admin-confirm-no:hover{background:rgba(255,255,255,.08);color:rgba(148,163,184,.9)}

/* -------------------------------------------------------------------
   NEURAX NEON FLIP CLOCK — 24h Hackathon Timer
   Classes: nx-outer, nx-top-label, nx-clock-row,
            nx-card-wrap, nx-card, nx-card-top/bottom/mid,
            nx-card-digits, nx-card-label, nx-scanline,
            nx-sep-wrap, nx-dot, nx-energy-bar/fill, nx-flip
------------------------------------------------------------------- */

/* --- Outer wrapper ----------------------------------------------- */
.nx-outer {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  margin: 36px 0 20px;
}

/* --- Live label -------------------------------------------------- */
.nx-top-label {
  display: flex;
  align-items: center;
  gap: 10px;
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(0.65rem, 1.3vw, 0.8rem);
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: #0891b2;
  text-shadow: 0 0 14px rgba(8,145,178,0.7);
}

.nx-live-dot {
  display: inline-block;
  width: 7px;
  height: 7px;
  background: #00f0ff;
  border-radius: 50%;
  box-shadow: 0 0 8px #00f0ff, 0 0 16px rgba(0,240,255,0.5);
  animation: nx-dot-pulse 1.2s ease-in-out infinite;
  flex-shrink: 0;
}

@keyframes nx-dot-pulse {
  0%,100% { transform: scale(1);   box-shadow: 0 0 8px #00f0ff, 0 0 16px rgba(0,240,255,0.5); }
  50%      { transform: scale(1.4); box-shadow: 0 0 14px #00f0ff, 0 0 28px rgba(0,240,255,0.7); }
}

/* --- Clock row --------------------------------------------------- */
.nx-clock-row {
  display: flex;
  align-items: center;
  gap: 12px;
}

/* --- Card wrapper (holds card + label below) --------------------- */
.nx-card-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
}

/* --- Card -------------------------------------------------------- */
.nx-card {
  position: relative;
  width: clamp(80px, 14vw, 120px);
  height: clamp(80px, 14vw, 120px);
  background: linear-gradient(160deg, #0a1628 0%, #0d1f38 50%, #071020 100%);
  border: 1px solid rgba(0, 240, 255, 0.25);
  border-radius: 14px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-shadow:
    0 0 0 1px rgba(0,240,255,0.08),
    0 0 20px rgba(0,240,255,0.10),
    0 0 50px rgba(8,145,178,0.08),
    inset 0 1px 0 rgba(255,255,255,0.06),
    inset 0 -1px 0 rgba(0,0,0,0.4);
  transition: box-shadow 0.25s ease;
}

.nx-card:hover {
  box-shadow:
    0 0 0 1px rgba(0,240,255,0.35),
    0 0 30px rgba(0,240,255,0.20),
    0 0 60px rgba(8,145,178,0.12),
    inset 0 1px 0 rgba(255,255,255,0.08);
}

/* Flip animation */
.nx-card.nx-flip {
  animation: nx-flip-anim 0.28s cubic-bezier(0.4,0,0.2,1);
}

@keyframes nx-flip-anim {
  0%   { transform: rotateX(0deg);   }
  40%  { transform: rotateX(-18deg); }
  100% { transform: rotateX(0deg);   }
}

/* --- Card halves ------------------------------------------------- */
.nx-card-top,
.nx-card-bottom {
  flex: 1;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.nx-card-top {
  align-items: flex-end;
  padding-bottom: 2px;
  background: linear-gradient(to bottom, rgba(0,240,255,0.04) 0%, transparent 100%);
}

.nx-card-bottom {
  align-items: flex-start;
  padding-top: 2px;
  background: linear-gradient(to top, rgba(0,0,0,0.3) 0%, transparent 100%);
}

.nx-card-bottom .nx-card-digits {
  transform: translateY(-50%);
  opacity: 0.7;
}

/* --- Digits ------------------------------------------------------ */
.nx-card-digits {
  font-family: 'Orbitron', sans-serif;
  font-size: clamp(2.2rem, 7vw, 4rem);
  font-weight: 900;
  letter-spacing: -3px;
  line-height: 1;
  /* Neon gradient text */
  background: linear-gradient(to bottom, #e0f7ff 0%, #38bdf8 40%, #0891b2 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  /* Glow via filter (works on -webkit-text-fill-color) */
  filter: drop-shadow(0 0 6px rgba(0,240,255,0.8)) drop-shadow(0 0 14px rgba(8,145,178,0.5));
  animation: nx-glow-pulse 2.4s ease-in-out infinite;
  display: block;
}

@keyframes nx-glow-pulse {
  0%,100% { filter: drop-shadow(0 0 6px rgba(0,240,255,0.8)) drop-shadow(0 0 14px rgba(8,145,178,0.5)); }
  50%      { filter: drop-shadow(0 0 12px rgba(0,240,255,1))  drop-shadow(0 0 28px rgba(8,145,178,0.8)); }
}

/* --- Mid divider line -------------------------------------------- */
.nx-card-mid {
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(to right, transparent 0%, rgba(0,240,255,0.55) 30%, rgba(0,240,255,0.55) 70%, transparent 100%);
  transform: translateY(-50%);
  z-index: 3;
}

/* --- Scan line sweep --------------------------------------------- */
.nx-scanline {
  position: absolute;
  top: -100%;
  left: 0;
  right: 0;
  height: 40%;
  background: linear-gradient(to bottom, transparent 0%, rgba(0,240,255,0.07) 50%, transparent 100%);
  animation: nx-scan 3.5s ease-in-out infinite;
  z-index: 2;
  pointer-events: none;
}

@keyframes nx-scan {
  0%   { top: -40%; }
  100% { top: 110%; }
}

/* --- Card label -------------------------------------------------- */
.nx-card-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(0.55rem, 1vw, 0.68rem);
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: rgba(0, 240, 255, 0.55);
  text-shadow: 0 0 10px rgba(0,240,255,0.3);
}

/* --- Separator dots ---------------------------------------------- */
.nx-sep-wrap {
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-self: center;
  padding-bottom: 24px;
}

.nx-dot {
  display: block;
  width: 6px;
  height: 6px;
  background: #00f0ff;
  border-radius: 50%;
  box-shadow: 0 0 8px #00f0ff, 0 0 16px rgba(0,240,255,0.4);
  animation: nx-dot-blink 1s ease-in-out infinite;
}

.nx-dot:nth-child(2) {
  animation-delay: 0.15s;
}

@keyframes nx-dot-blink {
  0%,100% { opacity: 1;   box-shadow: 0 0 8px #00f0ff, 0 0 16px rgba(0,240,255,0.4); }
  50%      { opacity: 0.2; box-shadow: none; }
}

/* --- Energy bar -------------------------------------------------- */
.nx-energy-bar {
  width: clamp(200px, 50vw, 360px);
  height: 3px;
  background: rgba(0,240,255,0.08);
  border-radius: 2px;
  overflow: hidden;
  margin-top: 4px;
}

.nx-energy-fill {
  height: 100%;
  background: linear-gradient(to right, #059669, #0891b2, #00f0ff);
  border-radius: 2px;
  box-shadow: 0 0 8px rgba(0,240,255,0.6);
  transition: width 1s linear;
}

/* --- Ended state ------------------------------------------------- */
.nx-ended {
  font-family: 'Orbitron', sans-serif;
  font-size: clamp(1.4rem, 4vw, 2rem);
  font-weight: 900;
  letter-spacing: 3px;
  background: linear-gradient(to right, #0891b2, #059669);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: nx-glow-pulse 2s ease-in-out infinite;
}

/* --- Mobile ------------------------------------------------------ */
@media (max-width: 480px) {
  .nx-clock-row { gap: 6px; }
  .nx-card      { border-radius: 10px; }
  .nx-sep-wrap  { gap: 7px; }
  .nx-dot       { width: 5px; height: 5px; }
  .nx-energy-bar{ width: 90vw; }
}
